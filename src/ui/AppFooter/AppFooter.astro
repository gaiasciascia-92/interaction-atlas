---
// AppFooter — footer minimo: nome, anno, contatto (COMPONENT_RULES.md).
// Contatto approvato dal curatore il 2026-07-08 (vedi CHANGELOG.md):
// link al sito, testo = dominio senza protocollo.
import PageShell from '../PageShell/PageShell.astro';
import Divider from '../Divider/Divider.astro';
import styles from './AppFooter.module.css';

interface Props {
  contactLabel?: string;
  contactHref?: string;
}

const {
  contactLabel = 'www.gaiasciascia.com',
  contactHref = 'https://www.gaiasciascia.com',
} = Astro.props;
const year = new Date().getFullYear();
---

<footer class={styles.footer}>
  <PageShell>
    <div class={styles.dividerRow}>
      <Divider />
    </div>
    <div class={styles.row}>
      <p class={styles.meta}>Interaction Atlas · {year}</p>
      {
        contactLabel && contactHref && (
          <a href={contactHref} class={styles.contact}>
            {contactLabel}
          </a>
        )
      }
    </div>
  </PageShell>
</footer>
