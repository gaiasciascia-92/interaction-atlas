# CHANGELOG — Interaction Atlas

Formato: [Keep a Changelog](https://keepachangelog.com/it/1.1.0/). Versioni: [SemVer](https://semver.org/lang/it/) a partire dal primo rilascio pubblico.

Questo file registra anche le **decisioni di design**: ogni deviazione dai documenti di progetto va annotata qui con data e motivazione.

---

## [Unreleased]

### Aggiunto
- 2026-07-08 — Documentazione completa di progetto (README, PROJECT_SPEC, DESIGN_SYSTEM, DESIGN_TOKENS, WIREFRAMES, USER_FLOW, COMPONENT_RULES, VISUAL_DNA, MOTION_PRINCIPLES, SILENT_UI, SEARCH, PLAYGROUND, COLLECTIONS, ROADMAP, TASK_001–010).
- 2026-07-08 — TASK_003: telaio completo. `PageShell`, `AppNav`, `AppFooter`, `SectionLabel`, `Divider`, `SkipLink` in `src/ui/`; `BaseLayout.astro` ora monta SkipLink → AppNav → `<main id="main-content">` → AppFooter con head SEO minimo e favicon. Pagina placeholder aggiornata per usare il telaio. Verificato: zero JS spedito sulle pagine statiche (nessuno script nel build output), tab order completo con skip-link funzionante, 360/768/1024/1500/1920 px (screenshot Playwright), nessuna animazione autonoma del telaio.
- 2026-07-08 — TASK_004: modello dati e contenuti campione. Content Collections `effects`/`catalog-components` con schema Zod fedele a `PROJECT_SPEC.md` §8 in `src/content.config.ts`; vocabolario DNA e validazione (4–8 termini, coppie oppositive, presenza Carattere/Energia) in `src/lib/dna.ts`; tipi condivisi `src/types/effect.ts` e `src/types/preview.ts` (contratto `PreviewModule`). 3 effetti campione con contenuto reale (non lorem ipsum), uno per livello di preview — `blur-reveal` (css), `hover-image-reveal` (gsap), `webgl-displacement-hover` (webgl) — schede complete (overview/build/knowledge/explore/DNA), `status: draft`, Insight marcati `[DA REVISIONARE — curatore]`. 3 moduli preview corrispondenti in `src/previews/` conformi al contratto mount/destroy/pause/resume; GSAP e Three.js installati come dipendenze e caricati solo via `import()` dinamico dentro i rispettivi moduli. Aggiunta l'integrazione `@astrojs/mdx` (necessaria perché Astro riconosca i file `.mdx` come voci di collection). Verificato con build intenzionalmente invalide (DNA fuori vocabolario, <4/>8 termini, coppia oppositiva, `ref` inesistente → build fallita con messaggio chiaro) e con un harness Playwright a runtime (mount/pause/resume/destroy sui 3 moduli: nessun canvas/rAF/listener residuo dopo `destroy()`).
- 2026-07-08 — TASK_005: Home. `CategoryRow`, `PreviewLite`, `EntryRow`, `SearchBar` in `src/ui/`; helper `publishedOrAll` in `src/lib/content.ts`. Sezione Indice (4 righe fisse, decisione F) con preview Lite in hover/focus (fade `--duration-fast`, uscita → 60% attenuata, mai un flash a vuoto); sezione "Aggiunti di recente" (`EntryRow`, riuso conforme a TASK_005.md); search visibile ma disabilitata. Mobile (≤768px): niente hover, thumbnail statica 64×40 per riga, search full-width. Verificato: zero GSAP/Three.js nel bundle Home (unico script è ~700 byte di JS vanilla inline, nessun chunk esterno), comportamento hover/dimmed/focus-tastiera con Playwright, `prefers-reduced-motion` (funzionalità preservata, transizione a 0s), **Lighthouse Performance mobile: 100/100** (lighthouse 13.4.0, throttling simulato, viewport 360×800@2x, contro il build di produzione su `astro preview` — criterio ≥90 superato).
- 2026-07-08 — TASK_006: Category Page (`/[category]/`), il task più delicato della V1. `EntryList` e `PreviewPane` (isole) in `src/ui/`, coordinate da un controller di selezione condiviso (`src/lib/category-selection.ts`, pub/sub, singleton per pagina indipendente da quale delle due isole idrata per prima) e da metadati categoria condivisi con Home (`src/lib/categories.ts`, include le 4 descrizioni approvate dal curatore). Split-pane con lista (col 1–5) e `PreviewPane` sticky (col 6–12); voltapagina orizzontale (uscita −6%/200ms/ease-out, entrata ±6%→0/360ms/ease-inout con ritardo 80ms, direzione semantica successivo/precedente); selezione sincronizzata con `?e=slug` via `history.replaceState`; primo effetto preselezionato = ultima voce aggiunta; mobile impilato con preview sticky 40vh e swipe orizzontale che non tocca lo scroll verticale nativo. Corretto `blur-reveal.ts`: sotto `prefers-reduced-motion` mostrava lo stato rivelato invece del primo frame (ora coerente con l'overlay "Avvia preview" di `PreviewPane`). Verificato con Playwright: direzione del voltapagina (`--enter-x` 6%/-6%), input a raffica (8 `ArrowRight` a 20 ms di distanza → un solo slot nel DOM a fine sequenza, nessun errore console), deep-link `?e=slug` e back del browser coerente, tab order completo (titolo di ogni riga è l'unico stop da tastiera, frecce globali confermate senza focus nella lista), swipe orizzontale mobile, `prefers-reduced-motion` (transform nullo, cross-fade via `--duration-page`, pulsante "Avvia preview" funzionante), zero richieste di rete contenenti "three" sulla pagina Visual Effects (unica categoria con una voce `webgl`).
- 2026-07-09 — TASK_007 Parte B: Effect Page (`/e/[slug]`). Flusso verticale unico, sei sezioni (Preview, Overview, Build, Knowledge, Explore, Insight) con etichette `--text-label` e divider tra le sezioni, mai tab/accordion (WIREFRAMES.md §3). Nuovi componenti in `src/ui/` (COMPONENT_RULES.md, tutti "no JS"): `DnaTags` (pill Geist Mono, click inerte — vedi Decisioni), `MetaField` (coppia etichetta·valore), `DifficultyDots` (5 punti Orange, `role="img"` + alt testuale "Difficoltà N su 5"), `ExternalLink` (↗, `target="_blank"` + `rel="noopener noreferrer"`), `InsightBlock` (corpo `--text-h3`, filetto Purple, blocco firmato), `PrevNextNav` (precedente/successivo, ordine interno alla categoria). `PreviewPane` esteso con `variant?: 'category' | 'effect'` (default `'category'`, zero cambi di comportamento per la Category Page): in `variant="effect"` nasconde il blocco titolo/riassunto/link e i pulsanti precedente/successivo interni (ridondanti con l'h1 della pagina e `PrevNextNav`), non è più `position: sticky` (blocco nel flusso, non pannello che segue lo scroll), aggiunge i controlli replay `⟳` e fullscreen `⤢` (icone Lucide via nuova dipendenza `lucide-static`, SVG inlineati a build-time, stroke forzato a 1.5px via CSS). Fullscreen sulla Fullscreen API nativa (`stageEl.requestFullscreen()`/`document.exitFullscreen()`): Esc lo chiude per garanzia di specifica del browser, non codice applicativo; lo stato (`aria-pressed`, icona, focus di ritorno sul pulsante) si sincronizza tramite l'evento `fullscreenchange`, unica fonte di verità indipendente da come si è usciti. Scorciatoia `f` (decisione H) con guardia su campi editabili. Replay rimonta la stessa voce senza voltapagina (non è un cambio di contenuto). Su Effect Page l'avvio di un effetto `webgl` segue la Parte A.3: parte da solo se la preview è già in viewport al caricamento (verificato: il campione `webgl-displacement-hover` monta Three.js senza richiedere il click, essendo in viewport al load). Route `src/pages/e/[slug].astro`, `getStaticPaths()` unisce `effects` e `catalog-components`; precedente/successivo calcolati con lo stesso criterio di ordinamento della Category Page (`addedAt` discendente, `publishedOrAll`) sulle sole voci della stessa categoria. Sezioni/campi condizionali: Explore non renderizza (né la sua sezione né il divider che la precede) se demo/source/video/article/relatedEffects/relatedComponents sono tutti assenti; stesso trattamento per `PrevNextNav` quando non esiste né precedente né successivo — nessun divider "orfano" prima del footer. Verificato con Playwright sulle 3 voci campione (`blur-reveal`, `hover-image-reveal`, `webgl-displacement-hover`) e 3 viewport (360/768/1500px, preview 4:3 su mobile confermata): zero errori console, JS solo per l'unica isola PreviewPane (nessun altro script), fullscreen keyboard-accessible (Tab raggiunge i pulsanti, `f` apre/chiude, `aria-pressed` coerente), replay senza errori né slot duplicati, `prefers-reduced-motion` (primo frame reale del modulo + overlay "Avvia preview", controlli replay/fullscreen nascosti, coerente con TASK_006), nessuna regressione sulla Category Page (meta/prevNext interni ancora presenti, nessun controllo replay/fullscreen trapelato).
- 2026-07-09 — TASK_008: ricerca. Indice JSON statico generato in build (`src/pages/search-index.json.ts`, endpoint `GET` prerenderizzato), campi e pesi 1:1 da `SEARCH.md` §3 (`title` 1.0, `dna` 0.9, `summary` 0.6, categoria 0.5, tecnologia 0.4, `popularizedBy` 0.3); stesso fallback `publishedOrAll` della Home. Logica condivisa in `src/lib/search.ts` (Fuse.js: `runSearch`, `extractDnaFilters`, raggruppamento, suggerimenti). `SearchOverlay` (`src/ui/SearchOverlay/`) è l'unica implementazione dei risultati, montata una volta in `BaseLayout`; `SearchBar` (Home, `readonly` + `data-search-trigger`), il bottone "Cerca ⌘K" di `AppNav` (ora abilitato) e i tag DNA (ora `<button>`, non più `<span>` inerte) aprono tutti lo stesso overlay tramite delega di eventi su `[data-search-trigger]` — nessuno di questi tre componenti porta script proprio, coerente con l'inventario COMPONENT_RULES.md. **Caricamento in due stadi** per non impattare il first load: un bootstrap di 775 byte (sempre presente, `SearchOverlay.astro`) osserva click/focus/`⌘K` e importa dinamicamente `runtime.ts` (Fuse.js incluso, 35 KB) solo alla prima interazione reale — verificato che né `/search-index.json` né il chunk runtime compaiono tra le richieste di rete prima di quel momento, su nessuna pagina. Comportamento: debounce 80ms, raggruppamento "Effetti"/"Components" (etichette testuali da SEARCH.md §4) max 7 per gruppo, primo risultato preselezionato, `↑↓` naviga, `↵` apre, `esc` chiude e restituisce il focus a chi l'aveva prima dell'apertura, `tab` salta al gruppo successivo quando ce n'è più di uno (altrimenti tab-trap standard tra i soli elementi focalizzabili del pannello). Termini DNA riconosciuti in corso di digitazione (solo a parola completata, mai a metà parola) diventano chip rimovibili («DNA: Organic ✕»); `Backspace` a campo vuoto rimuove l'ultima chip. Stati vuoto (5 termini DNA più frequenti nell'indice corrente + 3 voci recenti) e nessun-risultato (messaggio + termini DNA più vicini via fuzzy-match sul solo vocabolario, cliccabili) come da SEARCH.md §5 — mai un vicolo cieco, verificato anche per la query letterale "organic elegant" (nessuna voce campione ha entrambi i geni: stato nessun-risultato corretto, non un crash). Accessibilità: `role="combobox"` + `aria-expanded`/`aria-controls`/`aria-autocomplete` sull'input, `role="listbox"`/`option` sui risultati, `aria-activedescendant` aggiornato ad ogni navigazione, `aria-live="polite"` con conteggio ("3 risultati."). Verificato con Playwright: ranking multi-termine ("editorial calm" → Blur Reveal, che ha entrambi i termini, sopra Hover Image Reveal, che ne ha uno solo, nessuno escluso), apertura/chiusura da tutti e tre i trigger con ritorno del focus corretto in ognuno, nessuna regressione sul first load (Lighthouse Home: **99/100**, lighthouse 13.4.0, stessa metodologia di TASK_005).

### Decisioni
- 2026-07-09 — **TASK_007 Parte A, riconciliazioni approvate dal curatore.** Tre correzioni a documenti e codice, tutte riconciliazioni di decisioni curatoriali esplicite ricevute in questo turno (non interpretazione autonoma):
  1. **Voltapagina: l'implementazione corrente è quella benedetta.** Supersede la voce sotto "Aritmetica del voltapagina in `MOTION_PRINCIPLES.md` §3 non torna": il curatore ha confermato exit 200ms, entrata ritardata di 80ms dall'istante 0 della sequenza (non dalla fine dell'uscita), durata entrata 360ms, budget complessivo 440ms — esattamente quanto già implementato in TASK_006 e già in produzione. Corretti `MOTION_PRINCIPLES.md` §3 (testo dei passi 2–3, tabella) e `--duration-page` da 560ms a 440ms in `src/styles/tokens.css` e `DESIGN_TOKENS.md` §5. Nessuna modifica di codice necessaria: l'implementazione era già corretta, solo la documentazione era in errore.
  2. **`PROJECT_SPEC.md` §7 riformulato: "una sola preview Live/Heavy attiva per pagina"**, non "mai più di una preview Live/Heavy attiva contemporaneamente". Il breve overlap visivo del voltapagina (uscente in dissolvenza già in pausa + entrante) è legittimo, non un'eccezione da giustificare — coerente con la decisione TASK_006 "una sola preview Live montata = una sola *attiva*, non mai due istanze non ancora distrutte", ora promossa a testo della regola stessa invece di restare una nota di interpretazione.
  3. **WebGL su Category Page: fallback testuale sostituito da avvio manuale.** Supersede la voce sotto "Il livello Heavy non monta sulla Category Page": ora monta sempre (placeholder neutro + azione "Avvia preview"; al click il modulo Three.js reale si carica e monta). `PROJECT_SPEC.md` §7 aggiornato: Heavy = "Effect Page (avvio su viewport o manuale) e Category Page (solo avvio manuale)". Implementato in `PreviewPane.astro` con nuova prop `heavyAutoStart` (default `false` → gate manuale sempre, usata da Category Page; `true` → può avviarsi automaticamente al primo ingresso in viewport, riservata a Effect Page/TASK_007) e funzione `scheduleMount()` che sostituisce la vecchia `renderFallback()` testuale. Verificato con Playwright: zero richieste di rete contenenti "three" al caricamento della pagina Visual Effects, una sola dopo il click su "Avvia preview"; il comportamento `prefers-reduced-motion` per le voci non-Heavy (mount reale con proprio primo frame, non un placeholder generico) resta invariato rispetto a TASK_006.
- 2026-07-09 — **TASK_007 Parte B, interpretazioni conservative di ambiguità nei documenti.**
  1. **`DnaTags`: click inerte in questo turno, reso come `<span>` non interattivo, non `<button>`/`<a>`.** `COMPONENT_RULES.md` descrive il click come "ricerca filtrata", funzione che arriva con TASK_008 (`ROADMAP.md` M5). Offrire oggi un controllo cliccabile che non fa nulla violerebbe DESIGN_SYSTEM.md §6 (nessuno stato non previsto) e SILENT_UI regola 7 (nessuna sorpresa meccanica — un click senza effetto è esattamente quel tipo di sorpresa). Convertito in `<button>` funzionante in TASK_008 (vedi voce sotto).
  2. **`ExternalLink` riusato anche per Learning Resources (Knowledge), non solo per Explore.** L'inventario di `COMPONENT_RULES.md` scopa il componente a "Explore", ma Knowledge → Learning Resources è la stessa natura di contenuto (link esterno, stesso bisogno di `rel` corretti) e l'inventario non prevede un secondo componente per lo stesso pattern. Estendere l'uso di un componente già approvato è la lettura più conservativa, contro l'introdurne uno bespoke equivalente non documentato.
  3. **`InsightBlock` "firmato" reso solo dal trattamento tipografico** (corpo `--text-h3`, rientro, filetto Purple), senza una riga di firma testuale: lo schema contenuti (`content.config.ts`) non ha un campo per il nome del curatore, e inventarne uno per popolare una firma sarebbe una decisione di contenuto autonoma.
  4. **`PrevNextNav` mostra il titolo reale della voce vicina**, non la didascalia generica "effetto precedente/successivo" dell'ASCII-art in `WIREFRAMES.md` §3: l'istruzione del turno per questo componente specifica esplicitamente "mantiene il contesto", che il solo testo generico non garantirebbe. Non esercitato dalle 3 voci campione attuali (ciascuna sola nella propria categoria): la sezione — incluso il divider che la precede — non si renderizza quando né precedente né successivo esistono, verificato con Playwright.
- 2026-07-09 — **TASK_008, interpretazioni conservative di ambiguità nei documenti.**
  1. **Termine DNA riconosciuto = query strutturata (peso `dna` 0.9), non filtro booleano rigido.** Ambiguità tra SEARCH.md §4 ("query multi-termine: AND morbido") e la parola "filtro" nello stesso paragrafo per i termini DNA riconosciuti. Un filtro booleano rigido (voce inclusa solo se ha *tutti* i termini attivi nel proprio `dna`) avrebbe prodotto spesso zero risultati con un catalogo di poche voci — verificato empiricamente: nessuna delle 3 voci campione ha sia "Organic" sia "Elegant". Il criterio di accettazione di TASK_008.md descrive esplicitamente un ranking ("i risultati con entrambi i geni salgono in testa", non "sono gli unici visibili"): le chip restano rimovibili (✕, decisione G) ma il termine continua a contribuire come testo pesato sul campo `dna`, non come esclusione. Verificato con i contenuti campione: "editorial calm" mostra sia Blur Reveal (entrambi i termini, primo) sia Hover Image Reveal (un termine, secondo) — nessuno escluso.
  2. **`DnaTags` attivato: da `<span>` inerte (decisione TASK_007 Parte B qui sopra, superata da questa voce) a `<button>` con `data-search-trigger`.** TASK_008 costruisce la ricerca: il click ora apre `SearchOverlay` con quel termine come filtro attivo, esattamente la funzione descritta in COMPONENT_RULES.md ("click → ricerca filtrata") che in TASK_007 non esisteva ancora. Supersede anche la voce "Affordance di ricerca `[Cerca ⌘K]` come `<button disabled>`" (TASK_003): il bottone in `AppNav` è ora abilitato, stesso meccanismo `data-search-trigger`.
  3. **Caricamento in due stadi (bootstrap + `runtime.ts` via `import()` dinamico) non richiesto esplicitamente da SEARCH.md, ma necessario per soddisfare "nessun impatto sul first load" alla lettera.** SEARCH.md §2 chiede solo che l'*indice* sia lazy al primo focus; non menziona il peso del modulo JS del componente stesso. `SearchOverlay` include Fuse.js (~35 KB): spedirlo su ogni pagina fin dal first load (anche mai aperto) sarebbe comunque un "impatto" nel senso più ampio del criterio di accettazione ("nessun impatto sul first load delle pagine", non "nessun impatto sul caricamento dell'indice"). Stesso pattern già in uso nel progetto per i moduli preview (`import()` dinamico, MOTION_PRINCIPLES.md §4), esteso qui al componente stesso.
  4. **`SearchBar` (Home) non ha una propria implementazione dei risultati: è un trigger `readonly` che apre `SearchOverlay`.** L'inventario COMPONENT_RULES.md lo classifica "isola" ma non descrive un secondo motore di rendering dei risultati separato dall'overlay; costruirne uno duplicherebbe la logica (in tensione con "stessa logica condivisa", esplicitamente richiesto per questo turno) e violerebbe SILENT_UI regola 2 (mai due superfici di risultati vive contemporaneamente). `readonly` invece di `disabled`: il campo resta annunciato correttamente da screen reader e raggiungibile da tastiera, a differenza di un controllo disabilitato.
  5. **Sinonimi italiani di categoria (`CATEGORY_SEARCH_SYNONYMS` in `search-index.json.ts`) usati solo per il matching, mai mostrati in UI.** SEARCH.md §3 chiede esplicitamente che "effetti visivi" trovi `visual-effects`, ma le etichette di navigazione restano in inglese (decisione C, PROJECT_SPEC.md §12). I due requisiti non confliggono: il campo `categorySearchText` esiste solo nell'indice di ricerca (mai renderizzato), l'etichetta mostrata nei risultati resta `categoryLabel` (inglese, la stessa di `CATEGORY_META`).
  6. **Verifica screen reader tramite attributi ARIA via Playwright (`role`, `aria-expanded`, `aria-activedescendant`, `aria-live`), non con VoiceOver/NVDA reali.** L'ambiente di sviluppo non ha uno screen reader installato; TASK_008.md chiede esplicitamente quella verifica ("verifica con VoiceOver o NVDA"). Annotato come limite di verifica, non come criterio soddisfatto in pieno: il pattern combobox/listbox implementato segue la ARIA Authoring Practices Guide (focus reale sempre sull'input, opzioni virtuali via `aria-activedescendant`), ma una verifica con uno screen reader reale resta da fare.
  5. **`variant="effect"` di `PreviewPane` non è sticky** (`position: static`), a differenza del pannello Category Page: sulla Effect Page la preview è un blocco nel flusso editoriale verticale (WIREFRAMES.md §3, "si legge come un articolo di rivista"), non un riquadro che segue lo scroll mentre le altre sezioni scorrono sotto — comportamento mai richiesto e in tensione con SILENT_UI regola 7.
  6. **Icone Lucide inserite come SVG statici** (nuova dipendenza `lucide-static`, `?raw` import, nessun JS a runtime) per i controlli replay/fullscreen, non un componente icona generico: `DESIGN_SYSTEM.md` §5 nomina esplicitamente "Lucide" come sistema di icone del prodotto, non ancora usato in nessuna pagina prima di questo task.
- 2026-07-08 — **Descrizioni delle 4 categorie approvate dal curatore**, mostrate nell'intestazione della lista come da `WIREFRAMES.md` §2: Interactions "Hover, cursore, scroll, drag: come il sito risponde alla mano di chi guarda."; Motion "Transizioni, reveal, ritmo: come le cose entrano, escono e respirano."; Visual Effects "Grana, blur, distorsione, WebGL: la materia visiva dell'immagine."; Components "Blocchi riutilizzabili: hero, gallerie, griglie, navigazioni, footer." Centralizzate in `src/lib/categories.ts`, riusate anche da Home (sostituiscono l'oggetto `categoryLabels` locale introdotto in TASK_005).
- 2026-07-08 — **Il livello Heavy (WebGL/Three.js) non monta sulla Category Page.** `PROJECT_SPEC.md` §7 elenca "Heavy" come "Solo Effect Page"; `PreviewPane` per `preview.kind: "webgl"` mostra un testo statico neutro con rimando alla Effect Page invece di montare il modulo reale. Interpretazione letterale della colonna "Dove" della tabella, non un'estensione: al momento l'unica voce Visual Effects è `webgl-displacement-hover`, quindi quella categoria non ha ancora una preview Live in-pagina finché non esisterà un effetto `css`/`gsap` in quella categoria o finché TASK_007 non costruisce la Effect Page.
- 2026-07-08 — **Aritmetica del voltapagina in `MOTION_PRINCIPLES.md` §3 non torna** (200 + 80 + 360 dichiarati uguali a 560 ms, ma la somma è 640 ms; `--duration-page` è definito 560 ms in `DESIGN_TOKENS.md`). Implementati i tre valori espliciti così come scritti (200 ms uscita/ease-out; 360 ms entrata/ease-inout; 80 ms interpretato come `transition-delay` dell'entrata rispetto all'inizio della sequenza, non come pausa dopo la fine dell'uscita) — è la lettura standard di un "ritardo" in un contesto di transizione CSS, produce una sovrapposizione uscita/entrata coerente con la metafora dello sfogliare (altrimenti impossibile da realizzare), e la sequenza visiva si conclude a 440 ms. Da riconciliare nella documentazione: o `--duration-page` diventa 640 ms, o uno dei tre valori (200/80/360) cambia perché la somma torni a 560.
- 2026-07-08 — **"Una sola preview Live montata" = una sola *attiva*, non "mai due istanze non ancora distrutte".** Una lettura strettamente letterale del criterio TASK_006.md renderebbe impossibile il voltapagina stesso descritto in `MOTION_PRINCIPLES.md` §3, che richiede una breve sovrapposizione visiva tra uscente ed entrante. `PreviewPane` chiama `pause()` sul modulo uscente nell'istante in cui parte il voltapagina (rimosso dall'insieme "attivo" tracciato per l'assert dev-mode) e `destroy()` allo scadere della sua sola animazione di uscita (200 ms) — coerente con "la precedente viene distrutta a voltapagina concluso" (WIREFRAMES.md §2) letto come "concluso il proprio turno", non l'intera sequenza.
- 2026-07-08 — **I controlli "← precedente / successivo →" della Category Page non riusano il componente `PrevNextNav`.** L'inventario di `COMPONENT_RULES.md` scopa `PrevNextNav` a "Effect" (navigazione tra Effect Page complete, un'altra pagina); qui cambiano solo la selezione locale (`?e=slug`, nessuna navigazione). Implementati come bottoni propri di `PreviewPane`.
- 2026-07-08 — **Ordine della lista per `addedAt` discendente**, non specificato in `WIREFRAMES.md` §2: fa coincidere la voce preselezionata ("ultima aggiunta") con la prima riga visibile, evitando un marker di selezione isolato a metà lista con un ordinamento alfabetico non richiesto altrove.
- 2026-07-08 — **Ogni riga di `EntryList` ha un solo stop da tastiera reale: il titolo** (link vero verso la Effect Page). Il resto della riga è un'area di click JS per la selezione, non un secondo elemento interattivo annidato (non valido in HTML dentro/accanto a un `<a>` senza duplicare semantica). La selezione da tastiera passa dalle frecce globali ↑/↓/←/→, esplicitamente "da qualunque punto della pagina" in WIREFRAMES.md §2 — verificato che funzionano anche senza focus nella lista.
- 2026-07-08 — **Riordino mobile via CSS `order`** (preview sopra, lista sotto) mantenendo l'ordine nel DOM lista-poi-preview: tecnica responsive standard, non introduce una discrepanza problematica tra ordine di lettura e ordine visivo perché entrambe le aree restano autonomamente operabili in qualunque ordine.
- 2026-07-08 — **Corretto `blur-reveal.ts`: `prefers-reduced-motion` mostrava lo stato rivelato (finale) invece del primo frame**, in contrasto con `MOTION_PRINCIPLES.md` §6 ("mostrano il primo frame con un'azione esplicita"). Correzione tecnica minima per allinearlo al nuovo overlay "Avvia preview" di `PreviewPane`, non una modifica di design del modulo.
- 2026-07-08 — **Conteggi CategoryRow e "Aggiunti di recente": fallback a tutte le voci esistenti quando nessuna è `published`.** Oggi tutte e 3 le voci campione sono `draft` (in attesa di revisione curatore, TASK_004); filtrare rigorosamente per `published` mostrerebbe conteggi a zero e una sezione "Aggiunti di recente" vuota. `publishedOrAll()` (`src/lib/content.ts`) usa le sole voci `published` non appena ne esiste almeno una per collection, e nel frattempo mostra le voci esistenti. Da rimuovere/verificare quando esisteranno voci pubblicate.
- 2026-07-08 — **PreviewLite mostra solo nome + conteggio della categoria, non un effetto "rappresentativo".** WIREFRAMES.md §1 non specifica quale voce mostrare quando una categoria ne contiene più di una, né esiste un campo `preview.lite` a livello di categoria in PROJECT_SPEC.md §8 (solo per singolo effetto). Evitata qualunque logica di curatela automatica non specificata (es. "più recente", "più popolare").
- 2026-07-08 — **Placeholder neutro (solo opacità di Black) per PreviewLite e la thumbnail mobile di CategoryRow, nessuna variazione cromatica per categoria.** Nessun asset fotografico reale è disponibile; un gradiente Orange/Green per "colorare" ciascuna delle 4 categorie violerebbe DESIGN_SYSTEM.md §3 ("a riposo l'app è bicroma… Orange e Green sono eventi rari") ed è comunque un sistema di colore-per-categoria non specificato in alcun documento (VISUAL_DNA.md §Presentazione vieta esplicitamente il colore-per-categoria sui tag DNA). Purple (`--glow-subtle`) appare solo sul pannello attivo, come da DESIGN_SYSTEM.md §4.
- 2026-07-08 — **Orchestrazione hover/focus → preview Lite con un piccolo script vanilla nella pagina, non nei componenti.** `CategoryRow` e `PreviewLite` restano "no JS" come da inventario COMPONENT_RULES.md (nessuno dei due richiede script per rendersi da solo); il coordinamento cross-componente (quale preview è attiva) è un comportamento della pagina Home, non di un singolo componente. Criterio di accettazione TASK_005.md "Zero GSAP/Three.js sulla Home" (non "zero JS") conferma che JS non pesante è ammesso qui, a differenza del telaio (TASK_003). Le classi di stato `is-active`/`is-dimmed` sono `:global()` in `PreviewLite.module.css` per restare stabili e non hashate: è il solo modo per uno script vanilla di pilotare CSS Modules.
- 2026-07-08 — **`SearchBar` creato ora come componente statico e disabilitato**, non isola. TASK_005 chiede "input presente ma non funzionante"; TASK_008 (ROADMAP.md M5) è il task che costruisce la ricerca reale — `SearchBar` esiste già nell'inventario di COMPONENT_RULES.md, quindi si evolve sul posto invece di spostare markup dalla pagina al componente in un secondo momento.
- 2026-07-08 — **Href di CategoryRow (`/interactions` ecc.) ed EntryRow (`/e/[slug]`) puntano a rotte non ancora costruite** (Category Page: TASK_006; Effect Page: TASK_007) e restituiscono 404 fino ad allora. Conforme alla sequenza ROADMAP.md M4; non correggere costruendo in anticipo quelle pagine (niente astrazioni anticipate, COMPONENT_RULES.md).
- 2026-07-08 — **`reference()` di Astro non basta a validare "ref inesistente".** Convalida solo la forma della stringa, non l'esistenza dell'id nella collection referenziata — verificato empiricamente: una build con `explore.relatedEffects: ['id-inesistente']` passava senza errori. Aggiunta `src/pages/validate-content.astro`, rimossa dalla build di produzione con lo stesso meccanismo di `/styleguide` (vedi `astro.config.mjs`): è l'unico punto del progetto con accesso a `astro:content` fuori da un contesto Vite/pagina, quindi l'unico dove il controllo di esistenza può essere eseguito e far fallire `astro build` con un messaggio leggibile.
- 2026-07-08 — **`@astrojs/mdx` fissato a `^4.3.14`.** Unica major compatibile con `astro@^5` già installato (le versioni 5.x/6.x/7.x del pacchetto richiedono rispettivamente `astro@^6`/`^6`/`^7`). Correzione tecnica di versione, non di design.
- 2026-07-08 — **`PlaygroundParams` (`src/types/effect.ts`) minimale, non popolato in V1.** `PLAYGROUND.md` (V3) specifica solo la firma runtime di `setParams`, non la forma del campo `preview.params` nello schema contenuti; nessuna delle 3 voci campione lo dichiara, coerente con "V3, non sviluppata in V1" (`PROJECT_SPEC.md` §10).
- 2026-07-08 — **Placeholder procedurali nelle 3 preview campione**, al posto di fotografie reali: gradienti dalla palette del prodotto e una texture di rumore generata via canvas. Nessun asset fotografico è ancora disponibile (la fotografia demo è `[PROPOSTA]` aperta per il Playground V3, `PLAYGROUND.md`).
- 2026-07-08 — **Risorse citate in `knowledge.resources`/`build.libraries` verificate**, non generate: i link a Codrops, MDN, CSS-Tricks, GSAP e Three.js nelle 3 voci campione sono stati controllati uno a uno prima di essere inclusi nel contenuto.
- 2026-07-08 — **Nessuna voce campione in `catalog-components`.** TASK_004 richiede lo schema per entrambe le collection ma le 3 voci campione richieste sono tutte in `effects` (una per livello di preview, come da scope); `catalog-components` resta a zero voci — valido per una Content Collection, verrà popolata da un task successivo.
- 2026-07-08 — **Lista contenuti V1 approvata: 18 voci (4 Interactions, 4 Motion, 4 Visual Effects, 6 Components).** Sostituisce la distribuzione proposta 15–20/6+4+4+4–6 in `ROADMAP.md`. Elenco completo in `ROADMAP.md` § Piano contenuti V1.
- 2026-07-08 — **AppFooter, contatto valorizzato.** Risolve il placeholder registrato sotto "AppFooter, contatto non valorizzato": il curatore ha fornito il contatto reale — link a `www.gaiasciascia.com` (testo del link: il dominio, senza protocollo). Default impostato in `AppFooter.astro`, prop `contactLabel`/`contactHref` restano sovrascrivibili.
- 2026-07-08 — **Favicon, monogramma "IA".** Risolve il placeholder registrato sotto "Favicon placeholder": il curatore ha approvato un monogramma tipografico "IA" in Geist peso 550, colore ink su fondo bg, nessun altro elemento grafico. Il font è incorporato nell'SVG come sottoinsieme dei soli glifi I/A (la favicon è un asset standalone, senza accesso a `public/fonts/`).
- 2026-07-08 — **AppNav, variante mobile senza isola JS.** TASK_003 ammette un'isola nav mobile solo "se necessaria"; in V1 la nav non contiene un elenco di link da collassare (le sezioni si raggiungono dalla Home, non dalla nav — vedi WIREFRAMES.md), quindi la variante compatta è risolta interamente con CSS responsive. Zero JS sulle pagine statiche, criterio di accettazione soddisfatto nella forma più conservativa. Da rivedere se una futura versione della nav introduce un elenco di link da collassare.
- 2026-07-08 — **Affordance di ricerca `[Cerca ⌘K]` come `<button disabled>`.** Non funzionante finché TASK_008: reso letteralmente non interattivo (stato "disabilitato" di DESIGN_SYSTEM.md §6, opacità 40%, escluso dal tab order) invece di un link morto o di un elemento solo visivamente disattivato. Nessun adattamento del tasto modificatore (⌘ vs Ctrl) per OS: rimandato a TASK_008, quando la feature diventa un'isola interattiva.
- 2026-07-08 — **Favicon placeholder.** Nessun documento di progetto (VISUAL_DNA.md, DESIGN_SYSTEM.md) specifica un logomark. `public/favicon.svg` usa solo i due colori non negoziabili (`--color-bg`, `--color-ink`) e la lettera "A", senza forma o icona inventata. Da sostituire quando un mark reale sarà approvato dal curatore.
- 2026-07-08 — **AppFooter, contatto non valorizzato.** COMPONENT_RULES.md e WIREFRAMES.md richiedono "nome, anno, contatto" nel footer ma nessun documento specifica un contatto reale (email, profilo). Il componente espone `contactLabel`/`contactHref` opzionali; non renderizza nulla finché il curatore non fornisce il dato, per non spedire un'informazione inventata.
- 2026-07-08 — **Dimensione testo di navigazione/footer.** DESIGN_TOKENS.md §3 non definisce un token dedicato per la nav; scelto `--text-small` (14px, esistente) come lettura più quieta e coerente con il registro Silent UI, invece di `--text-body`. Proponibile come token `--text-nav` dedicato se in futuro serve un valore diverso.
- 2026-07-08 — **AppNav persistente via `position: sticky`.** PROJECT_SPEC.md §5 richiede che ogni sezione sia raggiungibile "in un'interazione da qualunque punto dell'app"; implementato con sticky positioning nativo (nessuna trasformazione o animazione allo scroll, coerente col divieto di "header che si trasforma" in MOTION_PRINCIPLES.md §5).
- 2026-07-08 — **Scroll ibrido sulla Category Page** al posto dello scroll orizzontale obbligatorio del brief: lista verticale nativa + linguaggio orizzontale nelle transizioni della preview. Motivazione: velocità di scansione, coerenza con Silent UI, accessibilità (approvato dal curatore).
- 2026-07-08 — **Roadmap fasata** V1 Catalogo → V2 Studio → V3 Laboratorio. Motivazione: il collo di bottiglia è il contenuto curato, non il codice (approvato dal curatore).
- 2026-07-08 — **Contenuti e UI in italiano**; bozze delle schede prodotte da Claude, Insight revisionati dal curatore (approvato dal curatore).
- 2026-07-08 — **Gerarchia preview Lite/Live/Heavy** con regola una-sola-preview-live-per-pagina. Motivazione: performance su hardware medio.
- 2026-07-08 — **Decisioni A–H chiuse dal curatore** (PROJECT_SPEC §12): approvate A (⌘K), B (Geist Mono per metadati), C (navigazione in inglese), D (rotte brevi `/e/[slug]`), E (niente dark mode fino a dopo V3), G (termini DNA come filtri di ricerca), H (`f` fullscreen + og-image tipografica). Respinta F: in Home si mostra solo ciò che è rilasciato — niente righe Collections/Playground prima delle rispettive fasi.
